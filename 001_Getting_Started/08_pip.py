
# ~ pip is your friend when you need to install any package from the plethora of choices available at the python package index (PyPI)

# pip search < query > # Searches for packages whose name or summary contains <query>

# $ pip install [package_name] # latest version of the package

# $ pip install [package_name]==x.x.x # specific version of the package

# $ pip install '[package_name]>=x.x.x' # minimum version of the package

# pip list --outdated # To get an overview of which of your installed packages have become outdated

# pip install [package_name] --upgrade # upgrade

# [py or python] -m pip install -U pip # upgrade pip (windows)

# pip install -U pip # On Linux or macOS X
